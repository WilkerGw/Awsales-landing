import type { NextConfig } from "next";


const nextConfig: NextConfig = {
    images: {
    domains: ['googleusercontent.com'], // Adicione o domínio da sua imagem aqui
  },
  /* config options here */
};

export default nextConfig;
